language: generic
env: SWIFT_VERSION=4.1.2

matrix:
  include:
    - os: linux
      dist: xenial
    - os: osx
      osx_image: xcode9.4

install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - make
  - make test
